diff --git a/backend/.gitignore b/backend/.gitignore
index 2dc23cb..0e12983 100644
--- a/backend/.gitignore
+++ b/backend/.gitignore
@@ -2,6 +2,7 @@
 /dist
 /node_modules
 /build
+/generated
 
 # Logs
 logs
diff --git a/backend/package.json b/backend/package.json
index 2da7946..d6fcc08 100644
--- a/backend/package.json
+++ b/backend/package.json
@@ -12,6 +12,16 @@
     "start:dev": "nest start --watch",
     "start:debug": "nest start --debug --watch",
     "start:prod": "node dist/main",
+    "installer:win": "npm run build:bundle && cp release/server.js nsis-installer-windows/server.js && cp .env nsis-installer-windows/.env && cd nsis-installer-windows && bash build.sh",
+    "prisma:generate": "npx prisma generate",
+    "prisma:migrate": "npx prisma migrate dev",
+    "prisma:reset": "npx prisma migrate reset --force",
+    "prisma:studio": "npx prisma studio",
+    "prisma:seed": "npx tsx prisma/seeds/seed.ts",
+    "build:bundle": "npx esbuild dist/src/main.js --bundle --platform=node --target=node18 --outfile=release/server.js --minify --external:@nestjs/microservices --external:class-transformer/storage",
+    "release": "npm run build && npm run build:bundle",
+    "manager-db:win": "npm run build && npx esbuild dist/src/core/scripts/manager-database.js --bundle --platform=node --target=node18 --outfile=release/manager-database.js --minify --packages=external && npx pkg release/manager-database.js --targets node18-win-x64 --output release/manager-database.exe",
+    "manager-db:linux": "npm run build && npx esbuild dist/src/core/scripts/manager-database.js --bundle --platform=node --target=node18 --outfile=release/manager-database.js --minify --packages=external && npx pkg release/manager-database.js --targets node18-linux-x64 --output release/manager-database-linux",
     "lint": "eslint \"{src,apps,libs,test}/**/*.ts\" --fix",
     "test": "jest",
     "test:watch": "jest --watch",
diff --git a/backend/prisma/schema.prisma b/backend/prisma/schema.prisma
index dc48b34..7212cd9 100644
--- a/backend/prisma/schema.prisma
+++ b/backend/prisma/schema.prisma
@@ -5,9 +5,136 @@
 
 generator client {
   provider = "prisma-client"
-  output   = "../src/generated/prisma"
+  output   = "../generated/prisma/client"
 }
 
 datasource db {
   provider = "mysql"
 }
+
+enum UserRole {
+  admin
+  operator
+}
+
+enum ContentMode {
+  shared
+  exclusive
+  exclusive_strict
+}
+
+enum TotemStatus {
+  online
+  offline
+}
+
+enum MediaStatus {
+  active
+  inactive
+}
+
+model Category {
+  id        Int      @id @default(autoincrement())
+  name      String   @unique @db.VarChar(100)
+  createdAt DateTime @default(now()) @map("created_at")
+
+  clients Client[]
+
+  @@map("categories")
+}
+
+model Client {
+  id         Int      @id @default(autoincrement())
+  name       String   @db.VarChar(255)
+  categoryId Int      @map("category_id")
+  createdAt  DateTime @default(now()) @map("created_at")
+  updatedAt  DateTime @updatedAt @map("updated_at")
+
+  category Category @relation(fields: [categoryId], references: [id])
+  totems   Totem[]
+  media    Media[]
+
+  @@index([categoryId])
+  @@map("clients")
+}
+
+model User {
+  id           Int      @id @default(autoincrement())
+  name         String   @db.VarChar(255)
+  email        String   @unique @db.VarChar(255)
+  passwordHash String   @map("password_hash") @db.VarChar(255)
+  role         UserRole @default(operator)
+  isActive     Boolean  @default(true) @map("is_active")
+  createdAt    DateTime @default(now()) @map("created_at")
+  updatedAt    DateTime @updatedAt @map("updated_at")
+
+  @@index([email])
+  @@map("users")
+}
+
+model Totem {
+  id          Int         @id @default(autoincrement())
+  clientId    Int         @map("client_id")
+  name        String      @db.VarChar(255)
+  location    String?     @db.VarChar(255)
+  serial      String      @unique @db.VarChar(100)
+  contentMode ContentMode @default(shared) @map("content_mode")
+  status      TotemStatus @default(offline)
+  lastSeenAt  DateTime?   @map("last_seen_at")
+  createdAt   DateTime    @default(now()) @map("created_at")
+  updatedAt   DateTime    @updatedAt @map("updated_at")
+
+  client       Client           @relation(fields: [clientId], references: [id])
+  credentials  TotemCredential[]
+  playbackLogs PlaybackLog[]
+
+  @@index([clientId])
+  @@map("totems")
+}
+
+model TotemCredential {
+  id         Int       @id @default(autoincrement())
+  totemId    Int       @map("totem_id")
+  deviceId   String    @unique @map("device_id") @db.VarChar(255)
+  tokenHash  String    @map("token_hash") @db.VarChar(255)
+  revokedAt  DateTime? @map("revoked_at")
+  createdAt  DateTime  @default(now()) @map("created_at")
+
+  totem Totem @relation(fields: [totemId], references: [id])
+
+  @@index([deviceId])
+  @@map("totem_credentials")
+}
+
+model Media {
+  id        Int         @id @default(autoincrement())
+  clientId  Int?        @map("client_id")
+  title     String      @db.VarChar(255)
+  filePath  String      @map("file_path") @db.VarChar(500)
+  checksum  String      @db.VarChar(64)
+  duration  Int
+  weight    Int         @default(1)
+  status    MediaStatus @default(active)
+  createdAt DateTime    @default(now()) @map("created_at")
+  updatedAt DateTime    @updatedAt @map("updated_at")
+
+  client       Client?       @relation(fields: [clientId], references: [id])
+  playbackLogs PlaybackLog[]
+
+  @@index([clientId])
+  @@index([status])
+  @@map("media")
+}
+
+model PlaybackLog {
+  id       Int      @id @default(autoincrement())
+  totemId  Int      @map("totem_id")
+  mediaId  Int      @map("media_id")
+  playedAt DateTime @default(now()) @map("played_at")
+
+  totem Totem @relation(fields: [totemId], references: [id])
+  media Media @relation(fields: [mediaId], references: [id])
+
+  @@index([totemId, playedAt])
+  @@map("playback_logs")
+}
\ No newline at end of file
